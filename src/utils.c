#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

int load_ROM_to_memory(uint8_t memory[], size_t memory_size, char* rom_filename) {
	printf("Loading ROM: %s\n", rom_filename);
	return 0;
}
